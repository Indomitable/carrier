[package]
name = "carrier"
version = "0.1.0"
edition = "2021"
description = "Universal package outdated checker"

[dependencies]
# CLI
clap = { version = "4", features = ["derive"] }

# HTTP (synchronous)
ureq = { version = "3", features = ["json"] }

# Serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
quick-xml = { version = "0.36", features = ["serialize"] }
toml = "0.8"

# Versioning
semver = "1"

# Error handling
anyhow = "1.0"

# Output
colored = "2"
indicatif = "0.17"
rayon = "1.12.0"

[dev-dependencies]
tempfile = "3.27.0"
